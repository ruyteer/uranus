{
  "name": "@uranus/agents",
  "version": "0.1.0",
  "description": "Uranus - runtime de agentes declarativos e catalogo.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "uranus-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "clean": "tsc -b tsconfig.build.json --clean"
  },
  "dependencies": {
    "@uranus/backlog": "workspace:*",
    "@uranus/core": "workspace:*",
    "@uranus/prompts": "workspace:*",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@uranus/events": "workspace:*",
    "@uranus/testkit": "workspace:*"
  }
}
